// Set
const numbers = new Set([1, 2, 3]);

console.log(numbers);

// Duplicate Values
const numbers = new Set([1, 2, 3, 2, 4, 3]);

// console.log(numbers);

// using has()
// const items = new Set([1, 2, 3]);

// console.log(items.has(2));
// console.log(items.has(5));

// unique elements
const myArray = [1, 2, 3, 4, 4, 5, 5, 6];

const uniqueElements = new Set(myArray);

console.log(uniqueElements);